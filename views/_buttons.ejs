<div class="buttons-wrapper">
    <a href="/" class="logo">Codebin</a>
    <div class="buttons">
        <a href="/create" class="btn">New</a>
        <% if (locals.canSave) { %>
            <button class="btn" type="submit">Save</button>
        <% } %>

        <% if (locals.id != null) { %>
            <a href="/<%= locals.id %>/duplicate" class="btn">Duplicate</a>
        <% } %>
    </div>
</div>